version: "3.9"

services:
  server:
    build:
      context: ./apps/server
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - MIN_LEAD_MS=200
      - ALLOWED_ORIGINS=http://localhost:5173
  client:
    build:
      context: ./apps/client
    ports:
      - "5173:80"
